# Pin these before the final run and report the versions in the papers.
# summary.json records the versions actually used for the reported run.
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.5
imbalanced-learn>=0.12
xgboost>=2.0
statsmodels>=0.14
matplotlib>=3.8
python-docx>=1.1
openpyxl>=3.1
jupyter>=1.0
pytest>=7.0
